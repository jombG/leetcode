package valid_palindrome

import (
	"fmt"
	"testing"
)

func TestValid(t *testing.T) {
	//require.True(t, isPalindrome("A man, a plan, a canal: Panama"))
	fmt.Println(isPalindrome("A man, a plan, a canal: Panama"))
}
